<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.financeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceApp">
        <activity android:name=".AddTransactionActivity"></activity>
        <!-- android:theme="@style/Theme.FinanceApp.NoActionBar" Used to remove the default actionBar -->
        <!-- We created the Theme manually in themes.xml -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.FinanceApp.NoActionBar">

            <!-- Moved from the Main Activity to here because we want this activity to show on launch -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>